import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    Button: {
      borderRadius: 10
    },
    TextInput:{
      height: 50,
      width : 300,
      borderWidth: 1,
      padding: 10,
      margin: 10,
      fontSize: 20,
  
    },
    Container: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
      textAlign : 'center',
      
    }
  });